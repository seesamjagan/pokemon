{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {},
  "devDependencies": {
    "lerna": "^8.1.8"
  },
  "scripts": {
    "lerna": "lerna",
    "build": "lerna run build",
    "start": "lerna watch -- yarn build --since"
  }
}
